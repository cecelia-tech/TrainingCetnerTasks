<Window x:Class="Sensors.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sensors"
        mc:Ignorable="d"
        Title="Sensors" Height="350" Width="500">
    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <ContentControl Content="{Binding SelectedViewModel}"/>

        <!--
        <StackPanel Grid.Column="0" Grid.Row="0"  Orientation="Horizontal" HorizontalAlignment="Center"  >
            <TextBlock FontSize="18" Foreground="DarkBlue" HorizontalAlignment="Center" TextAlignment="Center" VerticalAlignment="Center"> 
                Amazing Sensor Watcher
            </TextBlock>
        </StackPanel>-->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Orientation="Horizontal">
            <Button Width="200" Content="Home" Command="{Binding UpdateViewComand}" CommandParameter="Home" />
            <Button Width="200" Content="Create Sensor" Command="{Binding UpdateViewComand}" CommandParameter="NewSensor"/>
        </StackPanel>
    </Grid>
</Window>
